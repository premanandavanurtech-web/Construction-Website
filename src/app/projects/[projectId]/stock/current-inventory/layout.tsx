// src/app/projects/[projectId]/stock/current-inventory/layout.tsx
import { ReactNode } from "react";

export default function CurrentInventoryLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}